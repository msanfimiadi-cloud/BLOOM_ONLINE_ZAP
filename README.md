# BLOOM ONLINE ZAP

Собственный сервис онлайн-записи к партнёрам Bloom Club: маникюр, массаж, SPA, косметология и другие услуги.

## Первая рабочая версия

- Публичный каталог партнёров и индивидуальные страницы записи `/book/<slug>`.
- Выбор услуги, специалиста, даты и свободного времени.
- Расчёт свободных интервалов с учётом продолжительности услуги и рабочего графика.
- Защита от двойной записи и пересекающихся интервалов на стороне сервера.
- Подтверждение записи с адресом, специалистом, стоимостью и временем визита.
- Защищённый кабинет администратора `/dashboard`.
- Управление партнёрами, специалистами и услугами.
- Журнал визитов, статусы записей, фильтрация по партнёру и сводная статистика.
- Постоянное хранение организаций, услуг, специалистов и записей в отдельной SQLite-базе.
- Адаптивный русскоязычный интерфейс.
- Разделение ролей владельца и партнёров с серверной проверкой каждой операции.
- Выдача и отключение партнёрских доступов по email и паролю.
- Telegram-уведомления о новых записях, отменах и изменениях статуса.
- Журнал отправки уведомлений и тестовые сообщения.
- Настраиваемая привилегия Bloom Club и часовой пояс каждого партнёра.
- Отдельные ссылки и HTML-кнопки для карточек партнёров Bloom Club.
- Автоматический учёт клиентов, пришедших по клубной ссылке.
- Полноценный календарь записей с колонками специалистов.
- Ручное создание, перенос и самостоятельная отмена записей.
- Автоматический расчёт скидки для посетителей Bloom Club.
- Рабочие дни, перерывы, интервалы между клиентами, выходные и исключения.
- Привязка конкретных услуг к специалистам.
- Карточки клиентов, история визитов, заметки и стоп-лист.
- Редактирование и отключение партнёров, специалистов и услуг.
- Защита от записи на прошедшее время с учётом часового пояса.
- Ограничение частоты публичных записей и защита от спама.
- Безопасная персональная ссылка для отмены клиентской записи.

## Технологии

React 19, Next.js 16, TypeScript, SQLite и Drizzle ORM. Проект подготовлен для обычного Node.js-сервера и не привязан к сторонней платформе хостинга.

## Запуск

Требуется Node.js версии 22.13 или новее.

```bash
npm ci
npm run dev
```

Проверки:

```bash
npm run typecheck
npm test
npm run build
```

Создание и обновление локальной базы:

```bash
npm run db:migrate
```

Схема базы находится в `db/schema.ts`, миграции — в `drizzle/`. Файл базы задаётся переменной `DATABASE_PATH`; по умолчанию используется `data/bloom-online.sqlite`.

## Развёртывание на Timeweb

Проект рассчитан на отдельного Linux-пользователя `bloom-online`, каталог `/opt/bloom-online` и локальный порт `3101`. Это изолирует его от Bloom Club, даже если оба приложения находятся на одном VPS.

1. Клонируйте репозиторий в `/opt/bloom-online` и выполните `npm ci`.
2. Создайте `/etc/bloom-online.env` по примеру `deploy/bloom-online.env.example` и ограничьте доступ командой `chmod 600 /etc/bloom-online.env`.
3. Выполните `npm run db:migrate`, затем `npm run build`.
4. Установите `deploy/bloom-online.service` в `/etc/systemd/system/` и запустите сервис.
5. Установите конфигурацию `deploy/nginx-online.bloomclub.ru.conf`, проверьте Nginx и выпустите сертификат командой `certbot --nginx -d online.bloomclub.ru`.

Секреты, пароли и IP-адреса не хранятся в репозитории. Для резервной копии SQLite предусмотрена команда `npm run db:backup` и готовый ежедневный systemd-таймер в `deploy/`. Состояние приложения проверяется через `/api/health`.

## Telegram-уведомления

Добавьте токен существующего бота Bloom Club в переменную окружения `TELEGRAM_BOT_TOKEN` на сервере. В кабинете партнёра откройте раздел уведомлений и нажмите «Подключить Telegram»: сервис выпустит одноразовую ссылку на 15 минут, а бот автоматически привяжет личный диалог к нужному партнёру. После подключения можно отправить тестовое уведомление. Ручной ввод chat ID не требуется.

Владелец входит на `/login` с `OWNER_EMAIL` и `OWNER_PASSWORD` из `/etc/bloom-online.env`. Сессия подписывается отдельным `SESSION_SECRET`. Партнёрские пароли сохраняются только в виде стойких scrypt-хешей.

## Интеграция с Bloom Club

Bloom Online подключается к основному приложению Bloom Club через защищённый server-to-server API.

1. В карточке партнёра Bloom Club укажите ссылку вида `https://ваш-домен/book/luna-studio`.
2. Участница нажимает «Записаться онлайн». Bloom Club выпускает короткоживущий подписанный токен, не содержащий имя или телефон.
3. Bloom Online проверяет токен на сервере Bloom Club, получает актуальное состояние подписки и заполняет имя с телефоном.
4. Клубная скидка применяется только после повторной серверной проверки действующей подписки и совпадения телефона.

Переменные окружения Bloom Online:

```text
BLOOM_CLUB_API_URL=https://bloomclub.ru/api/v1
BLOOM_ONLINE_API_TOKEN=одинаковый-секретный-ключ-не-короче-32-символов
```

На сервере Bloom Club задайте тот же `BLOOM_ONLINE_API_TOKEN` и `BLOOM_ONLINE_PUBLIC_URL` с текущим адресом сервиса записи. После смены домена достаточно обновить URL в настройках обоих проектов. Без настроенного API записи доступны, но клубная скидка по простой ссылке не применяется.

Каждый клубный переход ограничен конкретным партнёром: токен другой карточки нельзя применить для получения скидки. После создания, переноса, отмены, завершения или неявки Bloom Online отправляет защищённое событие на `POST /api/v1/booking/events`; Bloom Club сохраняет его в аналитике соответствующей участницы и партнёра. Общий секрет никогда не передаётся браузеру.

## Юридические документы

На сайте доступны публичная оферта `/offer`, пользовательское соглашение `/terms`, политика конфиденциальности `/privacy`, отдельное согласие на обработку персональных данных `/personal-data-consent` и правила использования cookies `/cookies`.

При регистрации салона принятие оферты и согласие на обработку персональных данных подтверждаются отдельными, изначально не отмеченными галочками. При оформлении записи клиент отдельно подтверждает обработку своих данных. Подтверждения, версия документа и время принятия сохраняются в таблице `legal_consent_events`.

В документах по умолчанию используются подтверждённые реквизиты оператора Bloom Club: ИП Глущенко Анастасия Дмитриевна, ИНН 541007956565, ОГРНИП 323547600049744 и почта bloomclub.info@mail.ru. При необходимости их можно переопределить через переменные окружения `LEGAL_OPERATOR_NAME`, `LEGAL_OPERATOR_INN`, `LEGAL_OPERATOR_OGRN` и `LEGAL_CONTACT_EMAIL`. Частный адрес оператора на сайте не публикуется.

## Подготовка к запуску

1. После первого входа создайте реальных партнёров в административном кабинете. Демонстрационные записи в production отключены.
2. Задайте `OWNER_EMAIL`, `OWNER_PASSWORD` и `SESSION_SECRET`, прежде чем открывать сайт широкой аудитории.
3. Подключите `TELEGRAM_BOT_TOKEN`, если требуются уведомления.
4. Выдайте партнёрам доступ по email и временному паролю.
5. Убедитесь, что `online.bloomclub.ru` направлен на VPS, и подключите HTTPS.
6. Настройте `BLOOM_CLUB_API_URL`, общий `BLOOM_ONLINE_API_TOKEN` и `BLOOM_ONLINE_PUBLIC_URL` на сервере клуба.
7. Разместите партнёрские ссылки в карточках Bloom Club.

Онлайн-оплата и предоплата требуют отдельных учётных данных и подключения соответствующих платёжных систем.
